{
  "mcpServers": {
    "airflow": {
      "command": "uvx",
      "args": ["astro-airflow-mcp", "--transport", "stdio", "--airflow-project-dir", "${PWD}"]
    },
    "jupyter": {
      "command": "uvx",
      "args": ["data-jupyter"]
    }
  }
}
